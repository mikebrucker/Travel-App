import React from "react";
// import { TextField, Button } from "@material-ui/core";

const TempFormat: any = ({ handleChange, handleSubmit, state }) => {
  return <div>C to F</div>;
};

export default TempFormat;
